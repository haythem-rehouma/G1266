| #    | Fichier / Tutoriel                                                                                                   | Paramètre à tester                | Extrait de code à modifier / exécuter                                                                                                                                                   |
| ---- | -------------------------------------------------------------------------------------------------------------------- | --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 00.1 | 00 - c-est-quoi-MLFlow-partie-1.md                                                                                   | Var. d’env. `MLFLOW_TRACKING_URI` | **Bash** : `export MLFLOW_TRACKING_URI=http://127.0.0.1:5000` puis dans Python : `mlflow.set_tracking_uri(os.getenv("MLFLOW_TRACKING_URI"))`                                            |
| 00.2 | 00 - c-est-quoi-MLFlow-partie-2.md                                                                                   | Backend store (fichier vs DB)     | `mlflow server --backend-store-uri sqlite:///mlflow.db --default-artifact-root ./mlruns` (puis tester `--backend-store-uri file:mlruns`)                                                |
| 00.3 | 00 - c-est-quoi-MLFlow-partie-3.md                                                                                   | `--default-artifact-root`         | `mlflow server --backend-store-uri sqlite:///mlflow.db --default-artifact-root ./mlruns-exp1` (changer le dossier et observer les artefacts)                                            |
| 00.4 | 00-Installation de MLflow sur une VM Ubuntu 2204.md                                                                  | Port du serveur                   | `mlflow server --host 0.0.0.0 --port 5000` puis `--port 5001` et se connecter à l’UI correspondante.                                                                                    |
| 01.1 | 01 - explication de set_tracking_uri.md                                                                              | Type d’URI                        | `mlflow.set_tracking_uri("http://127.0.0.1:5000")` puis `mlflow.set_tracking_uri("file:mlruns_local")` et comparer.                                                                     |
| 01.2 | 01-Exécution d'une première expérience MLflow.md                                                                     | Hyperparam. CLI                   | `python train.py --alpha 0.3 --l1_ratio 0.1` puis d’autres combinaisons et observer `mlflow.log_param(...)`.                                                                            |
| 02   | 02-Exécution d'une expérience MLflow avec un fichier requirements.md                                                 | Contenu de `requirements.txt`     | Dans `MLproject` : `conda_env: conda.yaml` et dans `conda.yaml` modifier la ligne `- scikit-learn==1.3.0` (version différente) puis relancer le projet.                                 |
| 03   | 03-Exécution d'une expérience MLflow avec set_tracking_uri et get_tracking_uri.md                                    | URL de tracking                   | `mlflow.set_tracking_uri("http://127.0.0.1:5000")` puis tester `mlflow.set_tracking_uri("http://127.0.0.1:9999")` et afficher `print(mlflow.get_tracking_uri())`.                       |
| 04.1 | 04-Création d’une expérience MLflow.md                                                                               | `experiment_name`                 | `exp = mlflow.set_experiment("wine_experiment_v1")` puis `exp = mlflow.set_experiment("wine_experiment_v2")`.                                                                           |
| 04.2 | 04-Exécution d'une expérience MLflow avec create_experiment.md                                                       | `artifact_location`               | `exp_id = mlflow.create_experiment("exp_local", artifact_location="file:mlruns_local")` puis autre dossier : `"file:mlruns_alt"`.                                                       |
| 05   | 05-Exécution d'une expérience MLflow avec active_run et last_active_run.md                                           | `run_name` + `last_active_run()`  | `with mlflow.start_run(run_name="run_alpha_0.3"):` puis autre nom `run_alpha_0.7`; ensuite `print(mlflow.last_active_run())`.                                                           |
| 06   | 06-Exécution d'une expérience MLflow avec log_artifacts.md                                                           | `artifact_path`                   | `mlflow.log_artifacts("plots", artifact_path="diagnostics")` puis changer `"diagnostics"` en `"plots_v2"`.                                                                              |
| 07   | 07-Tutoriel Complet _ Utilisation de mlflow.set_tags dans une Expérience MLflow.md                                   | Tags                              | `mlflow.set_tags({"stage": "dev", "dataset": "wine"})` puis `{"stage": "prod", "dataset": "wine", "owner": "etudiant"}`.                                                                |
| 08   | 08-Tutoriel Complet _ Lancer Plusieurs Exécutions avec MLflow.md                                                     | Grille d’hyperparam.              | Boucle : `for alpha in [0.1, 0.5, 0.9]:` et `for l1_ratio in [0.1, 0.5, 0.9]:` puis tester une grille plus fine `[0.1,0.2,...,1.0]`.                                                    |
| 09   | 09-Tutoriel Complet _ Lancer Plusieurs Expériences avec MLflow.md                                                    | Nom dynamique d’expérience        | `exp_name = f"elasticnet_alpha_{alpha}"` puis `mlflow.set_experiment(exp_name)` et comparer dans l’UI.                                                                                  |
| 10   | 10-Tutoriel Complet _ Utilisation de mlflow.autolog avec MLflow.md                                                   | Options d’autolog                 | `mlflow.sklearn.autolog(log_models=True)` puis `mlflow.sklearn.autolog(log_models=False, log_input_examples=True)`.                                                                     |
| 11   | 11-Tutoriel Complet _ Ajout d'une base de données (Parties 1 et 2).md                                                | URI DB                            | `mlflow server --backend-store-uri sqlite:///mlflow.db` puis tester `postgresql://user:pwd@host:5432/mlflow` (ou version simulée).                                                      |
| 12   | 12-model+signature+and+input+examples.md                                                                             | Colonnes d’entrée                 | `input_example = X_test.iloc[:1]` puis sélectionner sous-ensemble : `input_example = X_test[["alcohol","density"]].iloc[:1]`.                                                           |
| 13   | 13-Enregistrement de Modèles avec Signature et Exemple d’Entrée dans MLflow.md                                       | Signature manuelle                | `signature = infer_signature(X_train, y_train)` puis `signature = ModelSignature(inputs=..., outputs=...)` définie à la main.                                                           |
| 14   | 14-MLflow Avancé : Définir Manuellement la Signature et l’Exemple d’Entrée d’un Modèle.md                            | `input_example`                   | `mlflow.sklearn.log_model(model, "model", signature=signature, input_example=X_train.iloc[:1])` puis utiliser un exemple extrême : `X_train.describe().loc[["min"]]`.                   |
| 15   | 15-MLflow Pyfunc Avancé : Enregistrement de Modèles Scikit-learn avec Wrapper Personnalisé et Environnement Conda.md | Contenu `conda_env`               | `conda_env = {"name": "mlflow-env", "dependencies": ["python=3.10", "scikit-learn==1.3.0"]}` puis tester une autre version de Python ou scikit-learn.                                   |
| 16   | 16-MLflow en Mode Pyfunc - Wrapper Scikit-learn, Joblib et Environnement Conda Personnalisé.md                       | Méthode de sérialisation          | Dans le wrapper : `joblib.dump(model, path)` vs `pickle.dump(model, open(path, "wb"))` et recharger via `joblib.load` ou `pickle.load`.                                                 |
| 17   | 17-MLflow Reload - Chargement et Évaluation d’un Modèle Pyfunc après Déploiement.md                                  | `model_uri`                       | `model = mlflow.pyfunc.load_model("runs:/<run_id>/model")` puis `model = mlflow.pyfunc.load_model("models:/ElasticNetWine/Production")`.                                                |
| 18   | 18-Évaluation Automatisée d’un Modèle MLflow avec mlflow.evaluate.md                                                 | `model_type`                      | `result = mlflow.evaluate(model_uri, data=test_df, targets="quality", model_type="regressor")` puis tester `"classifier"` et analyser la différence.                                    |
| 19   | 19-Évaluation Avancée d’un Modèle MLflow avec Métriques Personnalisées et Artefacts Visuels.md                       | Métriques custom                  | `mlflow.log_metric("rmse", rmse)` + fonction custom `log_custom_metric("mae_norm", mae/norm)` puis comparer.                                                                            |
| 20   | 20-Évaluation Comparative avec Modèle de Base et Seuils de Validation dans MLflow.md                                 | Seuils                            | `if rmse < 0.6: mlflow.set_tag("status", "accepted") else: mlflow.set_tag("status", "rejected")` et modifier le seuil.                                                                  |
| 21   | 21-Enregistrement d’un Modèle ElasticNet avec MLflow et Déploiement via API.md                                       | `registered_model_name`           | `result = mlflow.register_model("runs:/<run_id>/model", "ElasticNetWine")` puis utiliser un autre nom de modèle.                                                                        |
| 22   | 22-Suivi et Enregistrement d’un Modèle ElasticNet avec MLflow et Validation des Chemins.md                           | Chemin des artefacts              | `mlflow.sklearn.log_model(model, artifact_path="elasticnet-model")` puis `artifact_path="models/elasticnet-v2"`.                                                                        |
| 23   | 23-Enregistrement et Chargement d’un Modèle Versionné avec MLflow.md                                                 | `stage` / version                 | `client.transition_model_version_stage("ElasticNetWine", version=1, stage="Staging")` puis `"Production"`.                                                                              |
| 24   | 24-Enregistrement d’un Modèle Externe dans MLflow avec cloudpickle.md                                                | Fonction de prédiction            | `def predict(pd_df): return my_model.predict(pd_df)` puis ajouter pré-traitement : `pd_df_scaled = scaler.transform(pd_df)`.                                                            |
| 25   | 25-Création d’une Expérience MLflow et Journalisation d’un Modèle ElasticNet.md                                      | Ordre des appels                  | Version A : `mlflow.set_experiment("exp_A"); mlflow.start_run()` ; version B : `with mlflow.start_run(run_name="test"): mlflow.set_experiment("exp_B")` et analyser l’expérience cible. |
| 26   | 26.md                                                                                                                | Paramètre libre                   | Exemples : `mlflow.log_param("optimizer", "adam")` vs `"sgd"` ou `mlflow.set_tag("note", "test_etudiant")`.                                                                             |
| 27   | 27-Exécution d’un Projet MLflow via mlflow.projects.run avec Paramètres Personnalisés.md                             | `parameters` de projet            | `mlflow.projects.run(".", parameters={"alpha": 0.5, "l1_ratio": 0.5})` puis d’autres valeurs.                                                                                           |
| 28   | 28-Commandes CLI Avancées pour la Gestion Complète de MLflow.md                                                      | Filtres CLI                       | `mlflow runs list --experiment-id 1` puis `mlflow runs list --experiment-id 1 --filter-tag "stage = 'prod'"`.                                                                           |
| 29   | 29-Commandes CLI Avancées pour la Gestion Complète de MLflow commentées.md                                           | Options CLI                       | `mlflow experiments list` ; `mlflow runs delete --run-id <id>` (sur environnement de test) pour observer l’effet sur l’UI.                                                              |
